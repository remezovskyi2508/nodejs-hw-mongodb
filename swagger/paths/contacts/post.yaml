tags:
  - Contacts
summary: Create contact
operationId: createContact
description: 'Create a contact with payload'
security:
  - bearerAuth: []
requestBody:
  content:
    multipart/form-data:
      schema:
        type: object
        required:
          - name
          - phoneNumber
          - contactType
        properties:
          name:
            description: "Contact's name"
            type: string
            example: 'John Doe'
          phoneNumber:
            description: "Contact's number"
            type: number
            example: +380978654539
          email:
            description: "Contact's email"
            type: string
            example: johndoe@gmail.com
          isFavourite:
            description: 'Mark if contact is favourite'
            type: boolean
            example: true
          contactType:
            description: "Contact's contact type"
            type: string
            enum: ['work', 'home', 'personal']
            example: home
          photo:
            description: "Contact's photo file to upload"
            type: string
            format: binary
responses:
  '201':
    description: Successfully created a contact!
    content:
      application/json:
        schema:
          type: object
          required:
            - status
            - message
            - data
          properties:
            status:
              type: integer
              example: 201
            message:
              type: string
              example: Successfully created a contact!
            data:
              type: object
              $ref: '../../components/schemas/contacts.yaml'
  '401':
    $ref: '../../components/responses/contacts/401.yaml'
